import firebaseService from '../firebase/firebaseService'

const initialState = {

}

export default (state = initialState, action) => {

    switch(action.type){
        default:
            return state;
    }

}