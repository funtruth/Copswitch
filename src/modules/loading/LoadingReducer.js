const initialState = {
    inLobby: false,
    inGame: false,
    place: null
}

export default (state = initialState, action) => {
    switch(action.type){
        default:
            return state;
    }
}